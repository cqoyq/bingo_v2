Project libbingo version history:
# v1.0.1_r1
	1. add new .h file in bingo/tcp/all.h.
 	2. add README file.
  
# v1.0.1_r2
  	1. modify Makefile.
  	2. add callback function thr_init_callback in bingo/thread/many_to_one.h 
		and bingo/thread/many_to_many.h.
  	  
# v1.0.1_r3
	1. add .gitignore file to git.

# v1.0.1_n1
	1. add log handler without postfix, file in bingo/log/file_logger_without_postfix.h.
	2. change log's level to LOG_LEVEL_ALL by default.
